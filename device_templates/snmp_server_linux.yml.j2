deviceList:
  - name: "{{ name | replace('-','') }}"
    profileName: "Linux Server"
    description: "{{ name | replace('-','') }}"
    labels: [ "iot" ]
    protocols:
      TCP:
        Address: "{{ address }}"
        Port: "161"
    autoEvents:
      - interval: "5s"
        onChange: true
        sourceName: "uptime"
